/**
 * UserHome
 *
 * Placeholder landing content for realm members under `/user/*` routes.
 *
 * Responsibilities:
 * - Provide a minimal home panel until product modules are wired for end users
 *
 * Related:
 * - `layouts/` user shell and navigation
 */
export const UserHome = () => (
  <div className="w-full">
    <p className="leading-relaxed text-slate-600">
      Member landing area. Use the menu for CRM lists and settings — add product modules here as your app grows.
    </p>
  </div>
);
