{
  "//": "Monorepo root — apps/* and packages/* are pnpm workspaces. `pnpm dev:all` runs DB migrations then starts web + API + worker in parallel (API also auto-migrates in development). `pnpm dev:web` uses Vite proxy to root `.env` API_PORT when `VITE_API_BASE_URL` is unset. `pnpm stop` gracefully stops registered dev PIDs and can kill other discovered app processes. `pnpm verify` = typecheck + test; `pnpm build:ci` adds `pnpm -r build`. Docs: docs/guidelines/README.md, docs/README.md.",
  "name": "saas-starter-pack",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "build": "pnpm -r build",
    "build:packages": "pnpm --filter \"./packages/*\" build",
    "verify": "pnpm typecheck && pnpm build:packages && pnpm test",
    "build:ci": "pnpm verify && pnpm --filter \"./apps/*\" build",
    "dev": "pnpm --filter @starter/api dev",
    "db:migrate": "pnpm --filter @starter/db db:migrate",
    "db:purge-test-fixtures": "pnpm --filter @starter/db db:purge-test-fixtures",
    "dev:all": "pnpm db:migrate && pnpm --parallel --filter @starter/web --filter @starter/api --filter @starter/worker dev",
    "dev:web": "pnpm --filter @starter/web dev",
    "dev:worker": "pnpm --filter @starter/worker dev",
    "dev:mobile": "pnpm --filter @starter/mobile start",
    "stop": "node scripts/stop-dev.mjs",
    "lint": "cross-env ESLINT_USE_FLAT_CONFIG=false pnpm -r lint",
    "lint:tests": "cross-env ESLINT_USE_FLAT_CONFIG=false eslint apps/api/test apps/worker/test packages/crypto/test packages/db/test packages/logger/test packages/shared/test e2e --ext .ts",
    "lint:src": "cross-env ESLINT_USE_FLAT_CONFIG=false eslint apps/api/src apps/web/src apps/worker/src apps/mobile packages/shared/src packages/db/src packages/crypto/src packages/logger/src --ext .ts,.tsx",
    "test": "pnpm -r test",
    "test:watch": "pnpm --filter @starter/web test:watch",
    "typecheck": "pnpm -r typecheck",
    "audit": "pnpm audit",
    "test:e2e": "playwright test",
    "test:e2e:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.0",
    "@starter/db": "workspace:*",
    "@types/node": "^24.0.0",
    "@types/pg": "^8.20.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "cross-env": "^10.1.0",
    "dotenv": "^16.6.1",
    "eslint": "^9.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
