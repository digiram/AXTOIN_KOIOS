/**
 * Prettier defaults for the whole repo (TypeScript, JavaScript, JSON, Markdown, YAML where applicable).
 * Editors pick this up automatically when the Prettier extension is enabled.
 */
module.exports = {
  semi: true,
  singleQuote: false,
  trailingComma: "none",
  printWidth: 100
};
